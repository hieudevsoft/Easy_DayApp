<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_marginBottom="10dp"
    android:layout_marginTop="10dp"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <ImageView
        android:id="@+id/image_Note"
        android:scaleType="fitCenter"
        android:layout_gravity="center_horizontal"
        android:src="@drawable/ic_home_me"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:contentDescription="TODO" />

    <EditText
        android:id="@+id/des_image_note"
        android:inputType="textMultiLine"
        android:textSize="16sp"
        android:fontFamily="@font/roboto"
        android:layout_margin="10dp"
        android:textColor="@color/colorTextWhite"
        android:textColorHint="@color/colorTextWhite"
        android:hint="Description"
        android:padding="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:backgroundTint="#DD282928"
        android:background="@drawable/custom_card_me" />
    <RelativeLayout
        android:layout_marginStart="10dp"
        android:layout_marginEnd="10dp"
        android:layout_marginBottom="10dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:textSize="14sp"
            android:fontFamily="@font/lato_regular"
            android:layout_alignParentStart="true"
            android:id="@+id/delete_imageNote"
            android:text="Delete"
            android:textColor="@color/colorTextWhite"
            android:background="@drawable/custom_button_image_note"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
        <Button
            android:textSize="14sp"
            android:fontFamily="@font/opensans"
            android:textColor="@color/colorTextWhite"
            android:background="@drawable/custom_button_image_note"
            android:id="@+id/submit_imageNote"
            android:text="Submit"
            android:layout_alignParentEnd="true"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>
    </RelativeLayout>

</LinearLayout>